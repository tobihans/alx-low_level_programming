#include <stdio.h>

/**
 * main - Using printf to print a string to stdout
 *
 * Return: 0 always
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
