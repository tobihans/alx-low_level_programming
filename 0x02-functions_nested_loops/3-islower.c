#include "main.h"

/**
 * _islower - Check is a char is in lowercase
 * @c: The character to check
 *
 * Returns: If lower 1.
 * If not 0 is returned.
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
