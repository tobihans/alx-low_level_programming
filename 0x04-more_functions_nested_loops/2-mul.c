#include "main.h"

/**
 * mul - Multiply two numbers together
 * @a: the first number
 * @b: the second number
 *
 * Return: THe result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
